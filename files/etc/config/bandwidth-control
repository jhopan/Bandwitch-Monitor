config main 'main'
	option enabled '1'
	option interval '60'
	option reset_day '1'

# Add devices and groups in LuCI: Services > Bandwidth Control.
# Quota counts download + upload in nlbwmon current period.
# Static IP is optional; quota and block always use MAC.
